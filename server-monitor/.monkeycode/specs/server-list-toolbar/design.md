# 服务器列表工具栏设计

Feature Name: server-list-toolbar
Updated: 2026-07-17

## 设计

工具栏使用水平 Flex 布局。搜索框设置为可收缩的弹性区域，状态筛选项使用紧凑的固定内容宽度容器。筛选按钮复用页面的半透明背景、细边框和圆角视觉语言。

## 验证

- 搜索框、全部、在线和离线筛选项出现在同一行。
- 搜索输入可用并支持清空。
- 三种状态筛选仍可切换且显示对应数量。
